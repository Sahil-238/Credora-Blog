import React from 'react';

const LoadMethod = () => {
  return (
    <div className="chapter-content">
      <h1>jQuery Load Method</h1>
      <section>
        <h2>Understanding the load() Method</h2>
        <p>
          The jQuery load() method is a simple way to load data from a server and insert it into an element.
          Learn how to load HTML content, handle different response types, and work with callbacks.
        </p>
        {/* Add more content here */}
      </section>
    </div>
  );
};

export default LoadMethod; 